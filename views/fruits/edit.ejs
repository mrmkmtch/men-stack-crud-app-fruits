<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit <%= fruit.name %></title>
</head>
<body>
    <h1>Edit <%= fruit.name %></h1>
        <form action="/fruits/<%= fruit._id %>?_method=PUT" method="POST">
                <label for="name">Name:</label>
                <input type="text" id="name" name="name" value="<%= fruit.name %>">
        
                <label for="color">Color:</label>
                <input type="text" id="color" name="color" value="<%= fruit.color %>">
        
                <div class="checkbox-div">
                    <label for="ready-to-eat">Ready to Eat:</label>
                    <input type="checkbox" id="ready-to-eat" name="isReadyToEat" <%= fruit.isReadyToEat ? "checked" : "" %>>
                </div>
        
                <button type="submit">Update Fruit</button>
        </form>

        <a class="fruits-index-link" href="/fruits">
            <div class="index-link-text">Back to Fruits</div>
        </a>
</body>
</html>